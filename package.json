{
  "name": "iknow-pdks",
  "version": "1.0.0",
  "private": true,
  "description": "IKnow Technology PDKS Portal (React Frontend + PHP Laravel Backend)",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "serve": "cd backend && php artisan serve",
    "deploy-backend": "git subtree push --prefix backend origin deploy"
  }
}
